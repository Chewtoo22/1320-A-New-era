# MongoDB connection string
MONGODB_URL=mongodb://localhost:27017

# MongoDB database name
DATABASE_NAME=turbo_showdown

# Comma-separated list of allowed CORS origins, or "*" to allow all
ALLOWED_ORIGINS=*
